import { InjectionToken } from '@angular/core';
import { Overlay } from '@angular/cdk/overlay';
import { Gallery } from '@ngx-gallery/core';
import { LightboxConfig } from './lightbox.model';
import { Lightbox } from './lightbox.service';
export declare function lightboxFactory(config: LightboxConfig, gallery: Gallery, overlay: Overlay): Lightbox;
export declare class LightboxModule {
    static forRoot(config?: LightboxConfig): {
        ngModule: typeof LightboxModule;
        providers: ({
            provide: InjectionToken<LightboxConfig>;
            useValue: LightboxConfig;
        } | {
            provide: typeof Lightbox;
            useFactory: (config: LightboxConfig, gallery: Gallery, overlay: Overlay) => Lightbox;
            deps: (InjectionToken<LightboxConfig> | typeof Gallery | typeof Overlay)[];
        })[];
    };
}

import { OnDestroy, OnInit, OnChanges, ElementRef, EventEmitter } from '@angular/core';
import { GalleryConfig, GalleryState } from '../models';
import { BehaviorSubject } from 'rxjs/BehaviorSubject';
import { Observable } from 'rxjs/Observable';
export declare class GalleryThumbComponent implements OnInit, OnChanges, OnDestroy {
    private _el;
    mc: any;
    thumbState$: Observable<any>;
    stateStream$: BehaviorSubject<{
        value: number;
        active: boolean;
    }>;
    state: GalleryState;
    config: GalleryConfig;
    indexChange: EventEmitter<string | number>;
    height: any;
    width: any;
    constructor(_el: ElementRef);
    ngOnChanges(): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    thumbsStyle(delta: number): {
        transform: string;
        width: string;
        height: string;
    };
    verticalPan(e: any): void;
    horizontalPan(e: any): void;
}

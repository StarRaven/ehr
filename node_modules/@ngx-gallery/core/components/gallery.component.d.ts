import { OnDestroy, OnInit, OnChanges, EventEmitter } from '@angular/core';
import { Gallery } from '../services/gallery.service';
import { GalleryRef } from '../services/gallery-ref';
import { GalleryItem, GalleryState } from '../models';
export declare class GalleryComponent implements OnInit, OnChanges, OnDestroy {
    private _gallery;
    galleryRef: GalleryRef;
    id: string;
    items: GalleryItem[];
    gestures: boolean;
    panSensitivity: number;
    zoomOut: number;
    dots: boolean;
    thumb: boolean;
    nav: boolean;
    counter: boolean;
    thumbWidth: number;
    thumbHeight: number;
    disableThumb: boolean;
    thumbPosition: 'top' | 'left' | 'right' | 'bottom';
    slidingDirection: 'horizontal' | 'vertical';
    destroyRef: boolean;
    indexChange: EventEmitter<GalleryState>;
    itemsChange: EventEmitter<GalleryState>;
    private _itemChange$;
    private _indexChange$;
    constructor(_gallery: Gallery);
    private getConfig();
    onIndexChange(i: any): void;
    ngOnChanges(): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    load(items: GalleryItem[]): void;
    add(item: GalleryItem, active?: boolean): void;
    remove(i: number): void;
    next(): void;
    prev(): void;
    set(i: number): void;
    reset(): void;
}

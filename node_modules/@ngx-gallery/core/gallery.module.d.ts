import { InjectionToken } from '@angular/core';
import { Gallery } from './services/gallery.service';
import { GalleryConfig } from './models';
export declare function galleryFactory(galleryConfig: GalleryConfig): Gallery;
export declare class GalleryModule {
    static forRoot(config?: GalleryConfig): {
        ngModule: typeof GalleryModule;
        providers: ({
            provide: InjectionToken<GalleryConfig>;
            useValue: GalleryConfig;
        } | {
            provide: typeof Gallery;
            useFactory: (galleryConfig: GalleryConfig) => Gallery;
            deps: InjectionToken<GalleryConfig>[];
        })[];
    };
}

/** This directive enable tap if HammerJS is loaded, otherwise it falls back to normal click event */
import { ElementRef, EventEmitter, OnDestroy, OnInit, Renderer2 } from '@angular/core';
export declare class TapClickDirective implements OnInit, OnDestroy {
    private el;
    private renderer;
    mc: any;
    clickListener: any;
    tapClickDisabled: boolean;
    tapClick: EventEmitter<{}>;
    constructor(el: ElementRef, renderer: Renderer2);
    ngOnInit(): void;
    /** Enable gestures if hammer is loaded */
    setTapEvent(): void;
    ngOnDestroy(): void;
}
